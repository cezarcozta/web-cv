import React from 'react';

import { Container } from './styles';

const Contacts: React.FC = () => {
  return (
    <Container>
      <h1>Contacts</h1>
    </Container>
  );
};

export default Contacts;
