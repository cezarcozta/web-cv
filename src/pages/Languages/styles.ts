import styled from 'styled-components';

const Container = styled.div`
  flex: 1;
  align-items: center;

  li {
    margin-bottom: 5px;

    img {
      margin-right: 10px;
    }

    span {
      font-weight: bold;
    }
  }
`;

export default Container;
